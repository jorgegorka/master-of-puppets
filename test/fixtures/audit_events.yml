task_created:
  auditable: design_homepage (Task)
  actor: one (User)
  action: created
  project: acme
  metadata:
    title: Design homepage

task_advanced:
  auditable: design_homepage (Task)
  actor: acme_in_progress (Column)
  action: task_advanced
  project: acme
  metadata:
    from_column_id: 1
    to_column_id: 2
    reason: implementation complete

# Includes a system event with no actor — validates the nullability change.
system_throttle_dispatched:
  auditable: acme (Project)
  action: throttle_dispatched
  project: acme
  metadata:
    promoted_run_id: 1

column_created:
  auditable: acme_in_progress (Column)
  actor: one (User)
  action: column_created
  project: acme
  metadata:
    name: In Progress
    transition_policy: agent
