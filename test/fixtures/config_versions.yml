column_edit_version:
  project: acme
  versionable: acme_in_progress (Column)
  author: one (User)
  action: update
  snapshot:
    name: In Progress
    description: "Active work in progress."
    job_spec: "Move tasks forward by completing the work described."
  changeset:
    description:
      - "Active work."
      - "Active work in progress."

column_budget_version:
  project: acme
  versionable: acme_in_progress (Column)
  author: one (User)
  action: update
  snapshot:
    name: In Progress
    budget_cents: 5000
  changeset:
    budget_cents:
      - 2500
      - 5000
