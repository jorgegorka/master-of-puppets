# SwarmAssignment fixtures are constructed inline per test — the alpha
# mission starts in :planning state with no assignments until decompose!
# creates them, so seeding fixtures here would model an invalid state.
