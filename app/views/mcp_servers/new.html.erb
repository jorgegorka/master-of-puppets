<% content_for :title, "New MCP server" %>

<section class="pad max-width center">
  <header class="flex justify-between align-center">
    <h1>New MCP server</h1>
    <%= link_to "Cancel", mcp_servers_path, class: "btn btn-secondary" %>
  </header>

  <%= render "form", mcp_server: @mcp_server %>
</section>
